# 模块规格说明书 23 - 多语言国际化模块

> 多语言国际化 (i18n) 功能设计与实现规范

## 📋 模块概述

### 功能描述
为商品管理系统提供完整的多语言支持，允许用户在简体中文、繁體中文、English、Français四种语言之间切换，实现界面文本的实时翻译。

### 核心特性
- ✅ 4种语言支持（zh, zh-Hant, en, fr）
- ✅ 浏览器语言自动检测
- ✅ 语言偏好持久化（localStorage）
- ✅ 动态语言文件加载（按需加载）
- ✅ 全局语言选择器（桌面端+移动端）
- ✅ 零刷新即时切换

### 技术栈
- **i18n库**: react-intl v10.1.11 (FormatJS)
- **React版本**: 18.3.1
- **TypeScript**: 5.5.4

---

## 🏗️ 架构设计

### 整体架构

```
┌─────────────────────────────────────────────┐
│              main.tsx                        │
│    <LanguageProvider>                        │
│      <App />                                 │
│    </LanguageProvider>                       │
└─────────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼────────┐    ┌────────▼────────┐
│ LanguageProvider│    │   IntlProvider   │
│ - locale state  │    │ - messages      │
│ - setLocale()   │    │ - locale        │
│ - localStorage  │    │ - defaultLocale │
└───────┬────────┘    └────────┬────────┘
        │                      │
        │         ┌────────────┘
        │         │
    ┌───▼─────────▼────────┐
    │   useLanguage()      │
    │   useIntl()          │
    │   (React Hooks)      │
    └──────────────────────┘
              │
    ┌─────────┴─────────┐
    │                   │
┌───▼────┐    ┌────────▼──────┐
│ Navbar │    │ ProductFilter │
│ Footer │    │ ProductList   │
│ ...    │    │ ...           │
└────────┘    └───────────────┘
```

### 数据流

```
用户操作 → LanguageSelector
              │
              ▼ (setLocale)
        LanguageProvider
              │
              ▼ (update state + localStorage)
        useEffect → 动态import语言文件
              │
              ▼ (setMessages)
        IntlProvider
              │
              ▼ (provide context)
        useIntl().formatMessage()
              │
              ▼
        组件重新渲染（新语言）
```

---

## 📁 文件结构

```
frontend/src/
├── i18n/
│   ├── index.ts                    # 语言配置与检测工具
│   ├── LanguageProvider.tsx        # Context Provider + IntlProvider
│   └── locales/
│       ├── zh.json                 # 简体中文翻译
│       ├── zh-Hant.json            # 繁體中文翻译
│       ├── en.json                 # English翻译
│       └── fr.json                 # Français翻译
├── components/
│   ├── LanguageSelector.tsx        # 语言选择器组件
│   ├── LanguageSelector.module.css # 语言选择器样式
│   ├── Navbar.tsx                  # 集成语言选择器
│   ├── Footer.tsx                  # 国际化文本
│   ├── ProductFilter.tsx           # 国际化筛选器
│   └── ...
├── pages/
│   ├── ProductList.tsx             # 国际化商品列表
│   └── ...
└── main.tsx                        # 包裹LanguageProvider
```

---

## 🔧 核心实现

### 1. 语言配置 (i18n/index.ts)

#### 支持的语言列表
```typescript
export const SUPPORTED_LOCALES = ['zh', 'zh-Hant', 'en', 'fr'] as const

export type SupportedLocale = typeof SUPPORTED_LOCALES[number]

export const LOCALE_NAMES: Record<SupportedLocale, string> = {
  'zh': '简体中文',
  'zh-Hant': '繁體中文',
  'en': 'English',
  'fr': 'Français'
}

export const LOCALE_FLAGS: Record<SupportedLocale, string> = {
  'zh': '🇨🇳',
  'zh-Hant': '🇭🇰',
  'en': '🇬🇧',
  'fr': '🇫🇷'
}
```

#### 浏览器语言检测
```typescript
export function getBrowserLanguage(): string {
  if (typeof navigator === 'undefined') {
    return 'en'
  }

  const browserLang = navigator.language || (navigator as any).userLanguage || 'en'
  return mapToSupportedLocale(browserLang)
}

function mapToSupportedLocale(browserLang: string): SupportedLocale {
  const lang = browserLang.toLowerCase()

  // Chinese Simplified
  if (lang === 'zh' || lang === 'zh-cn' || lang === 'zh-sg') {
    return 'zh'
  }

  // Chinese Traditional
  if (lang === 'zh-tw' || lang === 'zh-hk' || lang === 'zh-mo') {
    return 'zh-Hant'
  }

  // French
  if (lang === 'fr' || lang === 'fr-fr' || lang === 'fr-ca') {
    return 'fr'
  }

  // Default to English
  return 'en'
}
```

#### 默认语言获取
```typescript
export function getDefaultLanguage(): SupportedLocale {
  if (typeof localStorage !== 'undefined') {
    const saved = localStorage.getItem('locale')
    if (saved && SUPPORTED_LOCALES.includes(saved as SupportedLocale)) {
      return saved as SupportedLocale
    }
  }

  return getBrowserLanguage() as SupportedLocale
}
```

---

### 2. LanguageProvider (i18n/LanguageProvider.tsx)

#### Context定义
```typescript
interface LanguageContextType {
  locale: SupportedLocale
  setLocale: (locale: SupportedLocale) => void
}

const LanguageContext = createContext<LanguageContextType | undefined>(undefined)
```

#### Provider实现
```typescript
export function LanguageProvider({ children }: LanguageProviderProps) {
  const [locale, setLocaleState] = useState<SupportedLocale>(() => {
    return getDefaultLanguage()
  })

  const [messages, setMessages] = useState<Record<string, string> | undefined>(undefined)

  // 动态加载语言文件
  useEffect(() => {
    let isMounted = true

    const loadMessages = async () => {
      try {
        const localeModule = await import(`./locales/${locale}.json`)
        
        if (isMounted) {
          setMessages(localeModule.default)
          localStorage.setItem('locale', locale)
        }
      } catch (error) {
        console.error(`Failed to load locale: ${locale}`, error)
        // Fallback to English
        if (locale !== 'en') {
          import('./locales/en.json').then(mod => {
            if (isMounted) {
              setMessages(mod.default)
            }
          })
        }
      }
    }

    loadMessages()

    return () => {
      isMounted = false
    }
  }, [locale])

  const setLocale = useCallback((newLocale: SupportedLocale) => {
    setLocaleState(newLocale)
  }, [])

  return (
    <LanguageContext.Provider value={{ locale, setLocale }}>
      <IntlProvider
        locale={locale}
        messages={messages}
        defaultLocale="en"
        onError={(error) => {
          if (import.meta.env.MODE !== 'development') {
            console.error('Intl error:', error)
          }
        }}
      >
        {children}
      </IntlProvider>
    </LanguageContext.Provider>
  )
}

export function useLanguage(): LanguageContextType {
  const context = useContext(LanguageContext)
  if (context === undefined) {
    throw new Error('useLanguage must be used within a LanguageProvider')
  }
  return context
}
```

---

### 3. LanguageSelector组件 (components/LanguageSelector.tsx)

#### 组件实现
```typescript
export default function LanguageSelector() {
  const intl = useIntl()
  const { locale, setLocale } = useLanguage()

  const handleChange = (e: React.ChangeEvent<HTMLSelectElement>) => {
    const newLocale = e.target.value as SupportedLocale
    setLocale(newLocale)
  }

  return (
    <div className={styles.languageSelector}>
      <select
        value={locale}
        onChange={handleChange}
        className={styles.select}
        aria-label={intl.formatMessage({ id: 'nav.language' })}
      >
        {SUPPORTED_LOCALES.map((loc) => (
          <option key={loc} value={loc}>
            {LOCALE_FLAGS[loc]} {LOCALE_NAMES[loc]}
          </option>
        ))}
      </select>
    </div>
  )
}
```

#### 样式规范 (LanguageSelector.module.css)
```css
.languageSelector {
  display: flex;
  align-items: center;
}

.select {
  padding: 6px 12px;
  background-color: white;
  border: 1px solid #d9d9d9;
  border-radius: 4px;
  font-size: 14px;
  color: #333;
  cursor: pointer;
  transition: all 0.3s;
  outline: none;
  min-width: 140px;
}

.select:hover {
  border-color: #1890ff;
}

.select:focus {
  border-color: #1890ff;
  box-shadow: 0 0 0 2px rgba(24, 144, 255, 0.2);
}

@media (max-width: 768px) {
  .select {
    min-width: 120px;
    font-size: 16px;
    padding: 8px 12px;
  }
}
```

---

## 📝 翻译文件规范

### 文件命名
- `zh.json` - 简体中文
- `zh-Hant.json` - 繁體中文
- `en.json` - English
- `fr.json` - Français

### Key命名规范
采用**点分命名法**（dot notation），按模块组织：

```
模块名.组件名.具体文本
```

#### 命名示例
```json
{
  "nav.logo": "商品管理系统",
  "nav.products": "商品列表",
  "nav.about": "关于我们",
  
  "filter.title": "筛选条件",
  "filter.keyword": "关键词搜索",
  "filter.price": "价格区间",
  
  "status.all": "全部",
  "status.on_sale": "在售",
  "status.off_sale": "下架",
  
  "common.loading": "加载中...",
  "common.empty": "暂无数据",
  "common.retry": "重试"
}
```

### 翻译文件结构
所有语言文件必须保持**相同的key结构**，确保翻译完整性。

---

## 🎨 UI集成

### 1. 导航栏集成 (Navbar.tsx)

#### 桌面端布局
```tsx
<div className={styles.navRight}>
  <ul className={styles.navLinks}>
    {/* 导航链接 */}
  </ul>
  
  {/* 语言选择器 */}
  <LanguageSelector />
</div>
```

#### 移动端布局
```tsx
<div className={styles.mobileMenu}>
  <ul className={styles.mobileNavLinks}>
    {/* 移动导航链接 */}
  </ul>
  
  {/* 移动端语言选择器 */}
  <div className={styles.mobileLanguageSelector}>
    <LanguageSelector />
  </div>
</div>
```

### 2. 组件中使用翻译

#### 基础用法
```typescript
import { useIntl } from 'react-intl'

export default function MyComponent() {
  const intl = useIntl()
  
  return (
    <h1>{intl.formatMessage({ id: 'page.title' })}</h1>
  )
}
```

#### 带参数的翻译
```typescript
// 翻译文件
{
  "product.results": "共 <strong>{total}</strong> 件商品"
}

// 组件中使用
<span dangerouslySetInnerHTML={{
  __html: intl.formatMessage(
    { id: 'product.results' },
    { total: pagination.total }
  )
}} />
```

#### 带变量的简单翻译
```typescript
intl.formatMessage(
  { id: 'product.searchKeyword' },
  { keyword: filters.keyword }
)
// 输出: 搜索关键词："iPhone"
```

---

## 🔄 工作流程

### 语言切换流程

```
1. 用户点击语言选择器
   ↓
2. 触发 onChange 事件
   ↓
3. 调用 setLocale(newLocale)
   ↓
4. LanguageProvider 更新 locale state
   ↓
5. useEffect 监听 locale 变化
   ↓
6. 动态 import 对应语言文件
   ↓
7. 更新 messages state
   ↓
8. 写入 localStorage 持久化
   ↓
9. IntlProvider 接收新 messages
   ↓
10. 所有使用 useIntl() 的组件重新渲染
   ↓
11. UI 显示新语言文本
```

### 首次加载流程

```
1. 应用启动 (main.tsx)
   ↓
2. LanguageProvider 初始化
   ↓
3. 调用 getDefaultLanguage()
   ↓
4. 检查 localStorage 是否有保存的语言
   ↓
5. 如果没有，检测浏览器语言
   ↓
6. 映射到支持的语言列表
   ↓
7. 加载对应语言文件
   ↓
8. 应用使用该语言渲染
```

---

## 🌍 语言映射表

### 浏览器语言 → 支持的语言

| 浏览器语言 | 映射到 | 说明 |
|-----------|--------|------|
| zh, zh-CN, zh-SG | zh | 简体中文 |
| zh-TW, zh-HK, zh-MO | zh-Hant | 繁體中文 |
| fr, fr-FR, fr-CA, fr-BE, fr-CH | fr | 法语 |
| 其他所有语言 | en | 英语（默认） |

---

## 📦 翻译覆盖范围

### 已国际化的模块

#### ✅ 导航与布局
- Logo/网站名称
- 导航链接（商品列表、关于我们、联系我们）
- 语言选择器标签

#### ✅ 筛选与搜索
- 筛选器标题
- 关键词搜索（标签、占位符）
- 价格区间（最低价、最高价）
- 商品状态（全部、在售、下架）
- 排序方式（上架时间、价格、名称、升序、降序）
- 按钮（搜索、重置）

#### ✅ 商品列表
- 页面标题
- 筛选按钮（显示/隐藏筛选）
- 重置筛选按钮
- 结果统计信息
- 空状态提示
- 加载提示

#### ✅ 页脚
- 公司名称
- 公司描述
- 快速链接标题和链接
- 联系方式标题
- 地址信息
- 版权信息

#### ✅ 通用组件
- 加载状态消息
- 空状态标题和操作按钮
- 错误提示

---

## ⚙️ 配置说明

### 添加新语言

#### 1. 更新支持的语言列表
```typescript
// i18n/index.ts
export const SUPPORTED_LOCALES = ['zh', 'zh-Hant', 'en', 'fr', 'ja'] as const
```

#### 2. 添加语言名称和国旗
```typescript
export const LOCALE_NAMES: Record<SupportedLocale, string> = {
  // ... existing
  'ja': '日本語'
}

export const LOCALE_FLAGS: Record<SupportedLocale, string> = {
  // ... existing
  'ja': '🇯🇵'
}
```

#### 3. 更新语言映射
```typescript
function mapToSupportedLocale(browserLang: string): SupportedLocale {
  // ... existing
  
  // Japanese
  if (lang === 'ja' || lang === 'ja-jp') {
    return 'ja'
  }
  
  // ...
}
```

#### 4. 创建翻译文件
```json
// i18n/locales/ja.json
{
  "nav.logo": "商品管理システム",
  "nav.products": "商品一覧",
  // ... 所有翻译key
}
```

---

## 🚀 性能优化

### 1. 动态导入
```typescript
// ✅ 按需加载，减少初始包体积
const localeModule = await import(`./locales/${locale}.json`)

// ❌ 不要一次性加载所有语言
import zh from './locales/zh.json'
import en from './locales/en.json'
// ...
```

### 2. 防抖清理
```typescript
useEffect(() => {
  let isMounted = true
  
  const loadMessages = async () => {
    // ...
    if (isMounted) {
      setMessages(localeModule.default)
    }
  }
  
  loadMessages()
  
  return () => {
    isMounted = false  // 防止组件卸载后更新state
  }
}, [locale])
```

### 3. useCallback优化
```typescript
const setLocale = useCallback((newLocale: SupportedLocale) => {
  setLocaleState(newLocale)
}, [])  // 空依赖，函数引用稳定
```

---

## 🧪 测试场景

### 功能测试

| 测试场景 | 预期结果 |
|---------|---------|
| 首次访问（浏览器语言zh-CN） | 自动显示简体中文 |
| 首次访问（浏览器语言en-US） | 自动显示English |
| 切换语言到繁體中文 | 所有文本立即切换 |
| 刷新页面 | 保持上次选择的语言 |
| 清除localStorage后刷新 | 重新检测浏览器语言 |
| 移动端切换语言 | 布局正常，选择器可用 |

### 边界测试

| 测试场景 | 预期结果 |
|---------|---------|
| 语言文件加载失败 | 降级到English |
| 不存在的翻译key | 显示key本身 |
| 快速连续切换语言 | 无竞态条件，显示最后选择的语言 |
| 不支持的浏览器语言 | 默认显示English |

---

## 🔐 安全性

### 1. XSS防护
使用`dangerouslySetInnerHTML`时确保内容来自可信任的翻译文件：
```typescript
// ✅ 安全：翻译文件由开发者控制
<span dangerouslySetInnerHTML={{
  __html: intl.formatMessage({ id: 'product.results' }, { total: 10 })
}} />

// ❌ 危险：不要使用用户输入
<span dangerouslySetInnerHTML={{
  __html: intl.formatMessage({ id: 'user.input' }, { value: userInput })
}} />
```

### 2. 类型安全
```typescript
// ✅ TypeScript保证locale类型正确
const setLocale = useCallback((newLocale: SupportedLocale) => {
  setLocaleState(newLocale)
}, [])

// ❌ 避免使用any
const setLocale = (locale: any) => { ... }
```

---

## 📊 统计信息

- **支持语言数量**: 4种
- **翻译文件总数**: 4个
- **翻译key总数**: ~110个
- **已国际化组件**: 6个（Navbar, Footer, ProductFilter, ProductList, Loading, Empty）
- **代码行数**: 
  - i18n核心: ~170行
  - LanguageSelector: ~40行
  - 翻译文件: ~450行（4个文件）

---

## 📚 相关文档

- [React Intl 官方文档](https://formatjs.io/docs/react-intl/)
- [FormatJS 文档](https://formatjs.io/docs/)
- [国际化最佳实践](https://formatjs.io/docs/guides/best-practices/)
- [模块22 - 导入/导出](./22-import-export.md)
- [模块21 - 管理后台](./21-admin-panel.md)

---

## ✅ 实现检查清单

- [x] 安装react-intl依赖
- [x] 创建4种语言翻译文件
- [x] 实现LanguageProvider
- [x] 实现语言选择器组件
- [x] 集成到main.tsx
- [x] 更新Navbar（桌面端+移动端）
- [x] 更新Footer
- [x] 更新ProductFilter
- [x] 更新ProductList
- [x] 浏览器语言自动检测
- [x] localStorage持久化
- [x] 动态语言文件加载
- [x] 错误降级（fallback到English）
- [x] 响应式布局适配
- [x] 类型安全（TypeScript）
- [x] 性能优化（动态导入、useCallback）

---

> **文档版本**: v1.0  
> **创建日期**: 2026-05-31  
> **最后更新**: 2026-05-31  
> **状态**: ✅ 已完成
